seed: 42
project_name: "PlantVillage-Baseline"
task_name: "convnet_dropout_intermediate"
tags: ["intermediate", "scratch", "dropout"]
device: "cuda"

data:
  dataset_name: "DScomp380/plant_village"
  image_size: 256
  subset_fraction: 0.25
  val_size: 0.15
  test_size: 0.15
  num_workers: 4
  normalize: true
  augment: false

model:
  num_classes: 39
  channels: [32, 64, 128]
  regularisation: "dropout"    # <-- use dropout
  dropout: 0.3                 # probability inside ConvBlocks

train:
  epochs: 30
  batch_size: 64
  optimizer: "adamw"
  lr: 0.001
  weight_decay: 0.0001

  patience: 3
  min_delta: 0.0

  scheduler:
    type: "cosine"
    T_max: 30

clearml:
  enabled: true
  project: "KCL-PlantVillage"
  task_name: "convnet_dropout_intermediate"
  output_uri: null
